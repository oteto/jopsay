{
	"tasks": {
		"cli": "deno run --allow-env=HOME,XDG_CONFIG_HOME --allow-read cli.ts",
		"lock:write": "deno cache --lock=lock.json --lock-write deps.ts",
		"lock:reload": "deno cache --reload --lock=lock.json deps.ts"
	}
}